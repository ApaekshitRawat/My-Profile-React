import { nameData } from "../data";
const lastSection = () => {
  return (
    <div className="lastSection">
      <h4>
        {nameData.firstName}
        {`'s-Portfoilo`}
      </h4>
    </div>
  );
};
export default lastSection;
